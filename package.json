{
  "name": "mpz",
  "version": "0.0.0",
  "description": "MPC library backed by privacy-scaling-explorations/mpz",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Andrew Morris",
  "license": "MIT",
  "dependencies": {
    "typescript": "^5.4.5"
  }
}
